/*var Animal = function (config) {
    this.name = config.name;
    this.type = config.type;
    this.age = config.age;

    this.makeNoise = () => {
        alert(config.sound);
    };
};

var max = Animal({
    name: 'Max',
    type: 'dog',
    age: 2,
    sound: 'Woof!'
});
max.makeNoise();*/